require 'spec_helper'

describe Peastash::Mongo do
  it 'has a version number' do
    expect(Peastash::Mongo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
